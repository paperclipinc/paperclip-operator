apiVersion: paperclip.inc/v1alpha1
kind: Instance
metadata:
  name: conf-ingress
spec:
  image:
    tag: v1.0.0
  database:
    mode: managed
  storage:
    persistence:
      enabled: true
      size: 1Gi
  networking:
    ingress:
      enabled: true
      hosts:
        - conf.example.com
